<%= link_to folders_path do %>
  <p>←Back</p>
<% end %>


<h2>Folder show page</h2>

<h2><%= @folder.name %></h2>
<%= link_to "edit folder name",edit_folder_path(@folder)  %>
<div class="cards">
  <% @folder.paragraphs.each do |p| %>
    <%= link_to folder_paragraph_path(@folder, p) do %>


        <div class="card-category">
          <p><%= p.name %></p>

<!--             <%#= link_to destroy_paragraph_path(@folder, p), method: :delete, data: {confirm: "Are you sure?"} do  %>
              <i class="fas fa-trash"></i>
            <%# end %> -->



        </div>


    <% end %>
  <% end %>
</div>

<p>Create quizess</p>
<%= simple_form_for @paragraph, :url => folder_paragraphs_path(@folder) do |f|  %>
  <%= f.input :name %>
  <%= f.input :sentence %>
  <%= f.submit %>
<% end %>
